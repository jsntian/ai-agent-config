# 我的全局开发约束

这些规则只描述个人长期工作习惯，不包含任何具体项目、仓库或任务细节。

## 沟通

- 默认使用中文回答。
- 先给结论，再说明依据和风险。
- 收尾统一报告：改了哪些文件（只读任务则报检查范围）、跑了什么验证命令、结果、未完成事项、剩余风险。

## 工作方式

- 要改代码时，先检查当前目录、Git 状态和项目约束文件（`AGENTS.md`、`CLAUDE.md`、`CONTEXT.md`）；非代码任务不必。
- 产品行为、数据语义或验收标准不明确，且无法从现有代码与契约推定：停下来，一句话说清问题和竞争选项，等确认。
- 只有不影响公开行为、数据语义、安全、权限、兼容性和验收结果的内部实现细节，才可以自己选一个、写明假设、继续。

## 质量

- 修改后执行与风险匹配的构建、测试、lint 或类型检查，测试范围按影响面圈定；涉及公共契约、跨模块行为或发布风险时执行全量。
- 验证结果三值报告：PASS / FAIL / **UNVERIFIED + 原因**。不得把「无法验证」写成 PASS 或 FAIL，不得声称执行过未实际执行的命令。
- 可以把真实凭据（密钥、Token、密码、私钥、证书）、生产地址或个人绝对路径写入代码、配置、日志或回复。
- 不使用破坏性 Git 命令（`push -f`、`reset --hard`、`clean -fd`、含 rebase 在内的任何历史改写），除非用户明确要求。普通 `git push` 可直接执行，但推向公共仓库、生产分支或发布 tag 属于公开发布，需一次确认。
- 优先使用小范围、可回滚的修改。

## 团队协作

- 项目负责人定义任务、范围和验收标准。不擅自创建、拆分或分配**团队**任务；为完成当前请求做的临时执行分解不受此限。多 Agent 派发按项目契约执行，契约未授权时不主动编排。

## 编码行为准则

### 先思考，再动手

- 先分析现状再改代码。不臆测，不掩盖不确定性，动手前说出假设；拿不准就按「工作方式」的判据决定是停下问还是写明假设继续。
- 有会导致**不同外部行为**的多种理解，全部列出来再选；只影响内部写法的自己定。
- 有更简单的方案就说出来，该反对时反对。

### 简单优先

- 只写解决问题所需的最少代码，不做投机性设计，不做需求之外的功能。
- 优先复用现有实现，避免无必要的抽象，不为一次性代码做抽象。
- 不添加未被要求的「灵活性/可配置性」。
- 不为不可能发生的场景写错误处理；但信任边界、安全、权限、数据完整性、并发和回滚场景不适用本条，一律要写。

### 精准修改

- 不扩大修改范围，不重写无关代码；只动必须动的地方，只清理自己制造的烂摊子。
- 改已有代码时不「顺手」改无关代码、注释或格式，不重构没坏的东西，风格保持与现有代码一致。
- 因本次改动产生的孤立 import/变量/函数要清理；本来就存在、与本次任务无关的死代码提一句，不要顺手删。
- 检验标准：每一行改动都能直接追溯到用户的这次请求。

### 目标驱动执行

- 把任务转化为可验证的成功标准，循环直到验证通过：「加校验」→ 先写无效输入的测试；「修 bug」→ 先写能复现的测试（无测试设施、纯配置修复或问题不可稳定复现时，说明原因并改用能跑的最小验证）；「重构 X」→ 确保前后测试都通过。
- 需要决策、跨模块或预计要多轮验证的任务，先给简要计划（步骤 → 验证方式）。

简化实现或压缩表达时永不砍掉：信任边界校验、防数据丢失的错误处理、安全、可访问性、用户明确要求的东西；永不压缩否定词、数字单位、代码块、错误串、路径、安全警告、PASS/FAIL/UNVERIFIED 及其原因。
